{
  "name": "ai-trends-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Use vercel dev locally'",
    "build": "echo 'No build step needed - Vercel auto-detects static + API'",
    "start": "echo 'Deployed on Vercel'"
  },
  "engines": {
    "node": "22.x"
  }
}
